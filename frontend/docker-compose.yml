services:
  frontend:
    build:
      dockerfile: Dockerfile
    networks:
      - it-works-network
    ports:
      - "3000:3000"
    environment:
      REACT_APP_BACKEND_URL: http://localhost:8080
