apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: it-works-postgres-data
  name: it-works-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
