apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: it-works-redis-data
  name: it-works-redis-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
